# Livt.Base Compiler Status

`Livt.Base` has no known package-specific compiler workarounds in its supported
public API. Every registered test component builds and completes its simulation.

Compiler issues discovered while maintaining this package are tracked in the
Livt release issue tracker rather than documented as package usage guidance.
